[package]
name = "agent-kernel-boot"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
agent-kernel = { path = "../agent-kernel" }
agent-kernel-core = { path = "../agent-kernel-core" }

[lib]
path = "src/lib.rs"
