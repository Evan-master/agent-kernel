[package]
name = "agent-kernel-hal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
agent-kernel-core = { path = "../agent-kernel-core" }

[lib]
path = "src/lib.rs"
