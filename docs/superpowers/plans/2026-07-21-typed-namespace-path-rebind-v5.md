# Typed Namespace Path Rebind V5 Plan

- [x] Audit V4 fixed-page transport, Core traversal and generation semantics,
  native executor reconciliation, and Resource Manager proof ownership.
- [x] Freeze the 160-byte typed envelope, message kind 1, Call 52 register
  contract, atomic Event sequence, and four-hop native mutation target.
- [x] Add failing Core and facade path-rebind contracts; typed decoder, Call 52,
  memory snapshot, and native evidence contracts continue in the ABI phase.
- [x] Implement the atomic Core path rebind transaction and receipt.
- [x] Implement typed call-data decoding, Call 52 authentication, snapshot,
  canonical reply, and executor reconciliation.
- [x] Extend the Resource Manager Capsule with the typed four-hop mutation and
  terminal identity, object, and revision validation.
- [x] Reconcile event capacity, transcript sequences, call counts, return
  offsets, generated Capsule bytes, hashes, and both README languages.
- [x] Run focused and Workspace tests, Supervisor simulation, `no_std`, strict
  Clippy, bare-metal checks, debug/release QEMU, and Release ELF audits.
- [ ] Commit the implementation and publish public `main` while the complete
  Agent Kernel goal remains active.
